app:
  version: 0.0.1
  formatted-version: "v${app.version}"
  java.version: "@java.version@"

spring:
  application:
    name: "Lambda Spring Boot (Test)"
  profiles.active: dev

logging:
  level:
    org.springframework.web: ERROR
    com.kapresoft: DEBUG

motto:
  house: "Tully"
  message: "Family, Duty, Honor"